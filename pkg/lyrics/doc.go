// Package lyrics provides reusable lyrics resolution and parsing helpers.
//
// It centralizes request normalization, provider chains, local and remote lookup
// strategies, cache wrappers, and LRC parsing so callers can consume a single
// lyrics model without caring where those lyrics came from.
package lyrics
