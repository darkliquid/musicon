# Responsibility

This parent node groups the concrete lookup strategies that implement the reusable cover-art provider contract.

For the active work area, it provides structural context for:

- local-first artwork providers
- remote fallback artwork providers

# Boundaries

This parent node is not responsible for terminal rendering, Musicon playback-screen orchestration, or defining the core metadata and chain abstractions it consumes.
