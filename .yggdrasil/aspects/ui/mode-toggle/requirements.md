# Dedicated mode requirement

The user explicitly chose dedicated fullscreen queue and playback modes instead of a persistent split view or an overlay-first design.

The UI must therefore present queue management and playback as distinct top-level screens within the same application shell.

Mode switching must be available through a dedicated key-driven interaction that keeps the current session in one mode at a time.
